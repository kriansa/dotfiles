;;; early-init.el --- Emacs early init file
;;;
;;; Commentary:
;;; This file is loaded whenever Emacs start, before init.el is loaded.
;;;
;;; Code:

;; Prevent emacs to load packages at startup
(setq package-enable-at-startup nil)

;;; early-init.el ends here
