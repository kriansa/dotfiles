modules = [
  "alacritty",
  "wezterm",
  "ghostty",
  "fish",
  "shell",
  "meta",
  "projects",
  "python",
  "borgmatic",
  "devops",
  "vlc",
  "git",
  "ruby",
  "tmux",
  "nvim",
  "javascript",
  "asdf",
  "golang",

  # Only for my Home desktop
  { name = "linux", scopes = "desktop" },
  { name = "alacritty-linux", scopes = "desktop" },
  { name = "gnupg", scopes = "desktop" },
  { name = "psi-notify", scopes = "desktop" },
  { name = "fontconfig", scopes = "desktop" },
  { name = "kde", scopes = "desktop" },
  { name = "zathura", scopes = "desktop" },

  # Incompatible with KDE
  # { name = "gnome", scopes = "desktop" },

  # Disabled
  # { name = "emacs" },

  # Only for Work
  { name = "macos", scopes = "work" },
  { name = "work", scopes = "work" },
  { name = "alacritty-mac", scopes = "work" },
  { name = "ghostty-mac", scopes = "work" },
]
