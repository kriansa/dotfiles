---
- name: install development packages
  become: true
  pacman:
    state: present
    name:
      # Editors
      - neovim
      - emacs
      # Cmake is required for emacs's vterm
      - cmake
      - dbeaver

      # Utilities
      - jq
      - yq
      - shellcheck
      - github-cli
      - python-cookiecutter

      # Shell utils
      - alacritty
      - fzf
      - bat
      - fd
      - exa
      - vivid
      - ripgrep
      - cowsay
      - ncdu

      # DB (docker to rule them all)
      # - mariadb
      # - postgresql

      # Python dev
      - python
      - python-pip
      - python-poetry

      # Java dev
      - jdk-openjdk
      - gradle

      # DevOps tools
      - aws-cli
      - ansible
      - terraform
      - python-dotenv
      - kubectl
      - kubectx

      # ---------------------
      # Packages on AUR/pacom

      # Common development SDKs
      - asdf-vm
      - devops-tools-git
      - ansible-bundler
      - heroku-cli
      - concourse-fly
