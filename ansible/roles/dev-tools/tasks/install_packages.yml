---
- name: install development packages
  become: true
  pacman:
    state: present
    name:
      # Editors
      - neovim
      - emacs
      - dbeaver

      # Utilities
      - jq
      - shellcheck
      - github-cli
      - python-cookiecutter

      # Shell utils
      - alacritty
      - fzf
      - bat
      - fd
      - exa
      - ripgrep
      - cowsay
      - ncdu

      # DB (docker to rule them all)
      # - mariadb
      # - postgresql

      # Python dev
      - python
      - python-pip
      - python-poetry

      # Java dev
      - jdk-openjdk
      - gradle

      # DevOps tools
      - aws-cli
      - ansible
      - terraform
      - python-dotenv
      - kubectl
      - kubectx

      # ---------------------
      # Packages on AUR/pacom

      # Common development SDKs
      - nodenv
      - nodenv-node-build-git
      - rbenv
      - ruby-build-git
      - devops-tools-git
      - ansible-bundler
      - heroku-cli
      - concourse-fly
