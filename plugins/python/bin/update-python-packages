#!/usr/bin/env bash

# Plugin path
PLUGIN_PATH=$(cd "$(dirname "$0")/.." || exit; pwd)

# Update pip
pip install --upgrade pip

while IFS= read -r package; do
  pip install "$package"
done < "$PLUGIN_PATH/data/packages.txt"

# Ensures that pyenv updates the binstubs
pyenv rehash
